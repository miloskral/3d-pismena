# ChannelLetters — Windows port (Tauri)

This is the Windows build of the ChannelLetters channel-letter DXF→STL tool. It
is a thin Tauri (Rust + WebView2) wrapper around the exact same encrypted HTML
bundle that ships with the macOS app (`channel-letters.enc`, AES-256-GCM).
